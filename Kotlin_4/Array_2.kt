package Kotlin_4

// fun booleanArrayOf(vararg elements : Boolean) : BooleanArray
// Boolean 타입을 갖는 배열을 생성함. 이 배열은 자바의 boolean[] 배열과 호환됨.

// fun byteArrayOf(vararg elements : Byte) : ByteArray
// Byte 타입을 갖는 배열을 생성함. 이 배열은 자바의 byte[] 배열과 호환됨.

// fun charArrayOf(vararg elements : Char) : CharArray
// Char 타입을 갖는 배열을 생성함. 이 배열은 자바의 char[] 배열과 호환됨.

// fun doubleArrayOf(vararg elements : Double) : DoubleArray
// Double 타입을 갖는 배열을 생성함. 이 배열은 자바의 double[] 배열과 호환됨.

// fun floatArrayOf(vararg elements : Float) : FloatArray
// Float 타입을 갖는 배열을 생성함. 이 배열은 자바의 float[] 배열과 호환됨.

// fun intArrayOf(vararg elements : Int) : IntArray
// Int 타입을 갖는 배열을 생성함. 이 배열은 자바의 int[] 배열과 호환됨.

// fun longArrayOf(vararg elements : Long) : LongArray
// Long 타입을 갖는 배열을 생성함. 이 배열은 자바의 long[] 배열과 호환됨.

// fun shortArrayOf(vararg elements : Short) : ShortArray
// Short 타입을 갖는 배열을 생성함. 이 배열은 자바의 short[] 배열과 호환됨.

fun main(args: Array<String>) {
    // Java 의 원시 타입 char 를 포함하는 배열을 생성함.
    val chars = charArrayOf('a', 'b', 'c', 'd')

    // Java 의 원시 타입 int 를 포함하는 배열을 생성함.
    val numbers = intArrayOf(1, 2, 3, 4)
}